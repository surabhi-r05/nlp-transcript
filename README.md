1. Start Ollama: `ollama serve`
2. Generate participants UI: `python src/generate_participants_ui.py`
3. Open `participants.html`, fill details, and save
4. Run the pipeline: `python src/main.py`
5. Check output in `output/workflow.json`
6. For DAG creation use `python src/visualization.py`
